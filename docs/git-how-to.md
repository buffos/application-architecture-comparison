# Commit Lesson

feat(domain-driven-design): complete lesson 023 product query surface

# Add tag

git tag -a domain-driven-design-023 -m "Domain-Driven Design Lesson 023" && git push origin --tags
git push origin domain-driven-design-023

or

git push origin --tags
