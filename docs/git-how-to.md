# Commit Lesson

feat(modular-monolith): complete lesson 018 return command idempotency

# Add tag

git tag -a modular-monolith-018 -m "Modular Monolith Lesson 018" && git push origin --tags
git push origin modular-monolith-018

or

git push origin --tags
