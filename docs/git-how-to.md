# Commit Lesson

feat(domain-driven-design): complete lesson 024 customer query surface

# Add tag

git tag -a domain-driven-design-024 -m "Domain-Driven Design Lesson 024" && git push origin --tags
git push origin domain-driven-design-024

or

git push origin --tags
