# Commit Lesson

feat(domain-driven-design): complete lesson 032 plugin pricing extension point

# Add tag

git tag -a domain-driven-design-032 -m "Domain-Driven Design Lesson 032" && git push origin --tags
git push origin domain-driven-design-032

or

git push origin --tags
