# Commit Lesson

feat(domain-driven-design): complete lesson 030 partial shipment support

# Add tag

git tag -a domain-driven-design-030 -m "Domain-Driven Design Lesson 030" && git push origin --tags
git push origin domain-driven-design-030

or

git push origin --tags
