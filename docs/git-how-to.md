# Commit Lesson

feat(component-based): complete lesson 010 shipment creation after payment

# Add tag

git tag -a component-based-010 -m "Component-Based Lesson 010" && git push origin --tags
git push origin component-based-010

or

git push origin --tags
