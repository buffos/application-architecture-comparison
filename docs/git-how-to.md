# Commit Lesson

feat(domain-driven-design): complete lesson 010 shipment aggregate after payment

# Add tag

git tag -a domain-driven-design-010 -m "Domain-Driven Design Lesson 010" && git push origin --tags
git push origin domain-driven-design-010

or

git push origin --tags
