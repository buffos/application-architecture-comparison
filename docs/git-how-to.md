# Commit Lesson

feat(component-based): complete lesson 027 low stock items report

# Add tag

git tag -a component-based-027 -m "Component-Based Lesson 027" && git push origin --tags
git push origin component-based-027

or

git push origin --tags
