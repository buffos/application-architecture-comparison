# Commit Lesson

feat(clean): complete lesson 028 orders awaiting approval report

# Add tag

git tag -a clean-028 -m "Clean Architecture Lesson 028"
git push origin clean-028

or

git push origin --tags
