# Commit Lesson

feat(modular-monolith): complete lesson 021 shipment query surface

# Add tag

git tag -a modular-monolith-021 -m "Modular Monolith Lesson 021" && git push origin --tags
git push origin modular-monolith-021

or

git push origin --tags
