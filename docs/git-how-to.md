# Commit Lesson

feat(microkernel): complete lesson 010 shipment creation after payment

# Add tag

git tag -a microkernel-010 -m "Microkernel Lesson 010" && git push origin --tags
git push origin microkernel-010

or

git push origin --tags
