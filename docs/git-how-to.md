# Commit Lesson

feat(component-based): complete lesson 005 approval policy component

# Add tag

git tag -a component-based-005 -m "Component-Based Lesson 005" && git push origin --tags
git push origin component-based-005

or

git push origin --tags
