# Commit Lesson

feat(microkernel): complete lesson 026 return rate by category report plugin

# Add tag

git tag -a microkernel-026 -m "Microkernel Lesson 026" && git push origin --tags
git push origin microkernel-026

or

git push origin --tags
