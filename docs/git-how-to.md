# Commit Lesson

feat(component-based): complete lesson 030 partial shipment support

# Add tag

git tag -a component-based-030 -m "Component-Based Lesson 030" && git push origin --tags
git push origin component-based-030

or

git push origin --tags
