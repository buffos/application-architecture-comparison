# Commit Lesson

feat(modular-monolith): complete lesson 023 product query surface

# Add tag

git tag -a modular-monolith-023 -m "Modular Monolith Lesson 023" && git push origin --tags
git push origin modular-monolith-023

or

git push origin --tags
