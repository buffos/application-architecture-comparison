# Commit Lesson

feat(component-based): complete lesson 006 approve pending quote

# Add tag

git tag -a component-based-006 -m "Component-Based Lesson 006" && git push origin --tags
git push origin component-based-006

or

git push origin --tags
