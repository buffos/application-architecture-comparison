# Commit Lesson

feat(domain-driven-design): complete lesson 027 low stock items report

# Add tag

git tag -a domain-driven-design-027 -m "Domain-Driven Design Lesson 027" && git push origin --tags
git push origin domain-driven-design-027

or

git push origin --tags
