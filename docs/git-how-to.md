# Commit Lesson

feat(modular-monolith): complete lesson 022 quote list query surface

# Add tag

git tag -a modular-monolith-022 -m "Modular Monolith Lesson 022" && git push origin --tags
git push origin modular-monolith-022

or

git push origin --tags
