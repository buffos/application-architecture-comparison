# Commit Lesson

feat(component-based): complete lesson 024 customer query surface

# Add tag

git tag -a component-based-024 -m "Component-Based Lesson 024" && git push origin --tags
git push origin component-based-024

or

git push origin --tags
