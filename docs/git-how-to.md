# Commit Lesson

feat(microkernel): complete lesson 024 customer query surface plugin

# Add tag

git tag -a microkernel-024 -m "Microkernel Lesson 024" && git push origin --tags
git push origin microkernel-024

or

git push origin --tags
